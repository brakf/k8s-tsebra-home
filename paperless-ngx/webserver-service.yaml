apiVersion: v1
kind: Service
metadata:
  name: paperless
  namespace: tsebra-home
spec:
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: paperless
